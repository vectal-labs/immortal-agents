# 0045 — Outage window ends when the API is ready

Status: accepted (David, 2026-09-03)

yes I think the outage window should end when the API is ready definitely so this is obvious because otherwise some of the harnesses especially Claude Code are struggling and they don't restart so yes we should do this until the stale DNS is solved until we can contact the API of that specific provider and then we send the keep going prompt
