# 0041 — Rename project to immortal-agents

Status: accepted (David, 2026-09-03)

The reason we chose a new name is because I want to open source this project soon and it means the previous name offline agent restart was not high perceived value enough. It didn't sound valuable enough. Sounded like an error state, you know, like some issue is with the agents. It didn't sound valuable and productive. So immortal agent. Immortal agents sounds much better, basically. Higher perceived value.

## Scope (added 2026-09-03)

Full rename: GitHub repo (`vectal-labs/immortal-agents`), local folder (`~/code/immortal-agents`), state dir (`~/.immortal-agents`, moved once by hand, no fallback in code), watcher launchd label (`com.davidondrej.immortal-agents`; netguard label unchanged, paths only), README, docs, and code strings. Old experiment captures stay as-is.
