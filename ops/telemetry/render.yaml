services:
  - type: web
    name: immortal-agents-telemetry
    runtime: python
    rootDir: ops/telemetry
    buildCommand: "true"
    startCommand: DATA_DIR=/data python3 server.py
    disk:
      name: events
      mountPath: /data
      sizeGB: 1
